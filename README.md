# financialadvisorslough — Page Package

This package contains the 4 files you asked for:

- `index.html` — main landing page
- `robots.txt` — crawler rules
- `sitemap.xml` — sitemap
- `README.md` — this file

## What's in index.html

- **Target keyword:** "Book mortgage appointment Slough" — used in the `<title>`, meta description, H1, and naturally through the body copy.
- **Word count:** 1000+ words of original, semantically structured article content (H1 → H2 → H3, lists, an FAQ block, a stats block).
- **Theme:** solid deep-green (`#0F2E27`) background with a WebGL / Three.js interactive particle network in the hero that reacts to mouse movement. No gradients — the base colour stays solid throughout, per your instruction.
- **Images:** 2 placeholder photographic images are included via Lorem Picsum (`picsum.photos`), each with descriptive alt text. Swap the `src` values for your own real photography before going live — placeholders are only there so the layout renders correctly.
- **Anchor link:** the only link in the article is the anchor text **"Click here"**, placed in the **second paragraph**, pointing to `https://www.cubicfinancial.com/`. No other link appears anywhere else in the article body.
- **No phone number or call button** anywhere on the page, as requested.
- `prefers-reduced-motion` is respected — the Three.js animation is skipped for users who have reduced motion turned on.

## Before you publish

1. Replace `https://www.financialadvisorslough.com/` in `index.html`, `robots.txt`, and `sitemap.xml` with your real live domain.
2. Swap the two `picsum.photos` placeholder images for real photos of your office/team/Slough area.
3. Update the `lastmod` date in `sitemap.xml` whenever the page content changes.
4. Upload all 4 files to the root of your hosting (same folder as your homepage).

---
**Urdu note:** Sab 4 files bana di gayi hain. `index.html` mein aap ka keyword, 1000+ words ka article, "Click here" anchor sirf **second paragraph** mein `cubicfinancial.com` ke link ke sath, WebGL/Three.js wala solid-colour interactive theme, aur images shamil hain. Phone number ya call button kahin add nahi kiya gaya. Publish karne se pehle domain aur placeholder images apni asal cheezon se replace kar lein.
